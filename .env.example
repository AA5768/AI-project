ANTHROPIC_API_KEY=

# Browser origins allowed to call the API, comma-separated. Leave empty on a dev
# machine: any localhost port is accepted, so the UI keeps working when Vite
# moves off 5173. Set an explicit list before running this anywhere else.
# CORS_ALLOW_ORIGINS=https://kb.example.com
